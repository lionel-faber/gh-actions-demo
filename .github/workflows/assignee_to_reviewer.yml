name: Assignee to reviewer

on: [pull_request]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - name: Assign a reviewer based on the assignee
      uses: lionel1704/assignee-to-reviewer-action@update-v2
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}