name: Triggered on request
on:
  pull_request_review:
    types: [submitted, edited]

jobs:
  run-testnet:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ toJson(github.review) }}"
      - run: echo "${{ toJson(github.review.body) }}"
      - run: echo "${{ github.review.body }}"
